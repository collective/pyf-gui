# Standards: By Relevance Sort

## Code Standards
- Use Svelte 5 runes (`$derived`, `$effect`)
- Follow existing naming conventions
- Keep TypeScript strict
- Maintain reactive store patterns

## UX Standards
- No auto-switching sort when search begins
- Clear fallback behavior when search cleared
- Option placement: top of dropdown when visible
